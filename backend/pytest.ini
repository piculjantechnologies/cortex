[pytest]
testpaths = tests
pythonpath = .
addopts = --strict-markers
markers =
    pipeline: offline pipeline tests (need pipeline/requirements.txt); the default run excludes them with -m "not pipeline"
